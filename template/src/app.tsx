import { observer } from 'mobx-react-lite';
import { KyLayoutFull } from '@tencent/koyi';

export default observer(() => (
  <KyLayoutFull className="l-index">
    <h2>hello koyi</h2>
  </KyLayoutFull >
));
